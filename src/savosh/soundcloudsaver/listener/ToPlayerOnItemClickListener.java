package savosh.soundcloudsaver.listener;


import android.view.View;
import android.widget.AdapterView;
import android.widget.Toast;
import savosh.soundcloudsaver.TrackPlayer;
import savosh.soundcloudsaver.model.Track;

public class ToPlayerOnItemClickListener implements AdapterView.OnItemClickListener {
    @Override
    public void onItemClick(AdapterView<?> parent, View view, int position, long id) {
//        Track track = adapter.getItem(position);
//        TrackPlayer.put(track);
//        Toast.makeText(getActivity(), "Add to player: " + track.getTitle(), Toast.LENGTH_SHORT).show();
    }
}